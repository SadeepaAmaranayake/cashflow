# For a physical phone, use the development computer's LAN IPv4 address.
# Preview and production builds must use the deployed API's public HTTPS URL.
# EXPO_PUBLIC values are bundled into the app and must never contain secrets.
EXPO_PUBLIC_API_URL=http://COMPUTER_IP:4000/api
